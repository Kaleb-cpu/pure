// next.config.js

const nextConfig = {
  images: {
    domains: ['picsum.photos', 'fastly.picsum.photos', 'images.pexels.com', 'www.pexels.com'], // Add 'fastly.picsum.photos' to the domains array
  },
};

export default nextConfig;
